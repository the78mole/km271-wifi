---
  esphome:
    name:         "${slug}"

  esp32:          #https://esphome.io/components/esp32.html
    board:        esp32dev
    framework:
      type:       esp-idf

  binary_sensor:
    - platform:   status
      name:       "Status"

  switch:
    - platform:   restart
      name:       "Restart"
